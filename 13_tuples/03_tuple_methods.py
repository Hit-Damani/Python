counting = (1, 2, 1, 25, 12)
print(counting.count(1))

print(counting.index(1)) # returns the index of first occurence if there are more then one

# why use tuples??
# tuples are faster than lists
# tuples are used as dictionary keys
# safe from unintentional changes