# sets are unordered and unique collections of elements, there is no duplicate elements

a = {1, 2, 3, 4, 5}
print(a, type(a))

# Sets store elements using a hash table, not sequential positions like lists.
# So elements are stored for fast lookup, not for ordering.

l = list(a)
print(l[0])


